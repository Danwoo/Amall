CREATE TABLE "ALARM" 
   (	"ALARM_ID" VARCHAR2(20 BYTE) NOT NULL ENABLE, 
	"SENDER_ID" VARCHAR2(20 BYTE) NOT NULL ENABLE, 
	"GETTER_ID" VARCHAR2(20 BYTE) NOT NULL ENABLE, 
	 CONSTRAINT "ALARM_PK" PRIMARY KEY ("ALARM_ID")
);